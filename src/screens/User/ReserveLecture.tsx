import {SafeAreaView, StyleSheet, Text, View} from 'react-native';
import React from 'react';

const ReserveLecture = () => {
  return (
    <SafeAreaView>
      <Text>ReserveLecture</Text>
    </SafeAreaView>
  );
};

export default ReserveLecture;

const styles = StyleSheet.create({});
